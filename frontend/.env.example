# Frontend (Next.js) env example
# Place client-visible envs here (NEXT_PUBLIC_*) and any frontend-only settings.
# Copy to .env.local in this same folder for local development.

NEXT_PUBLIC_CHAIN_ID=11155111
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_KAISIGN_CONTRACT_ADDRESS=
NEXT_PUBLIC_REALITY_ETH_ADDRESS=
NEXT_PUBLIC_METADATA_REGISTRY_ADDRESS=

# Subgraph endpoints
NEXT_PUBLIC_KAISIGN_GRAPH_URL=
NEXT_PUBLIC_REALITY_ETH_GRAPH_URL=

# Optional integrations
NEXT_PUBLIC_GTM=
NEXT_PUBLIC_ONETRUST=

# Optional: IPFS gateway (legacy/fallback)
NEXT_PUBLIC_IPFS_GATEWAY_URL=https://ipfs.io/ipfs

# Note: Server-side secrets (PRIVATE_KEY, KEYSTORE_JSON, SEPOLIA_RPC_URL) belong in the root env (.env.*) or your deployment platform secrets.
