FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci --only=production || npm install --production

# Copy application files
COPY eigenda-service.js ./
COPY eigenda-api.js ./

# Expose port
EXPOSE 3001

# Start the API server
CMD ["node", "eigenda-api.js"]